{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "winxi-server.Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn winxi.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
