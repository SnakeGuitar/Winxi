{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "winxi-server.Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'alembic upgrade head && python -u -m uvicorn winxi.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "restartPolicyType": "ON_FAILURE"
  }
}