# Backend API URL used by the Vite dev server proxy (local dev only)
API_URL=http://localhost:8000

# Backend URL exposed to the browser (required in production, e.g. Railway URL)
# VITE_API_URL=https://your-service.up.railway.app
